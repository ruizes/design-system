# 前端架构与组件调用关系

## 1. 项目概述

本项目是一个基于 Vue 3 + Vite 构建的设计系统展示项目，采用现代化的前端技术栈，提供完整的设计令牌、组件库和页面模板。

### 1.1 技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Vue | 3.4.0 | 前端框架 |
| Vue Router | 4.2.0 | 路由管理 |
| Vite | 5.0.0 | 构建工具 |
| Playwright | 1.40.0 | 端到端测试 |

### 1.2 项目结构

```
design-system/
├── src/
│   ├── pages/           # 页面组件
│   │   ├── Home.vue     # 首页
│   │   ├── Components.vue # 组件库页面
│   │   ├── Products.vue  # 产品展示页面
│   │   └── About.vue     # 关于我们页面
│   ├── styles/
│   │   └── main.css      # 全局样式与设计令牌
│   ├── App.vue           # 根组件
│   └── main.js           # 入口文件
├── tests/
│   └── design-system.spec.js # 端到端测试
├── index.html            # HTML 模板
├── vite.config.js        # Vite 配置
├── playwright.config.js  # Playwright 配置
└── package.json          # 项目依赖
```

## 2. 架构设计

### 2.1 整体架构

本项目采用单页应用 (SPA) 架构，通过 Vue Router 实现客户端路由，使用 Vite 作为构建工具实现快速开发和构建。

```
┌─────────────────────────────────────────────────────────────┐
│                         浏览器环境                             │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│  │   Vue 3      │  │ Vue Router   │  │  样式系统    │    │
│  │ (核心框架)   │  │ (路由管理)   │  │ (CSS变量)   │    │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘    │
│         │                   │                   │             │
│         └───────────────────┼───────────────────┘             │
│                             │                                 │
│                      ┌──────┴──────┐                         │
│                      │   页面组件   │                         │
│                      │  (Pages)    │                         │
│                      └──────┬──────┘                         │
│                             │                                 │
│              ┌──────────────┼──────────────┐                │
│              │              │              │                │
│         ┌────┴────┐    ┌────┴────┐    ┌────┴────┐         │
│         │  Home   │    │Components│    │ Products │         │
│         │  About  │    │           │    │          │         │
│         └─────────┘    └──────────┘    └──────────┘         │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 核心模块职责

#### 2.2.1 入口文件 (main.js)

**职责**：初始化 Vue 应用、配置路由、挂载根组件

**核心逻辑**：
1. 导入 Vue 3 的 `createApp` 函数
2. 导入 Vue Router 的 `createRouter` 和 `createWebHistory`
3. 定义路由配置数组，映射路径到页面组件
4. 创建路由实例并应用到 Vue 应用
5. 挂载应用到 DOM 元素

#### 2.2.2 根组件 (App.vue)

**职责**：应用的根容器，包含导航栏、页面容器和页脚

**主要功能**：
- 提供全局导航栏
- 管理响应式菜单状态
- 包含 `router-view` 用于渲染当前路由对应的页面
- 提供全局布局结构

#### 2.2.3 路由系统 (Vue Router)

**职责**：管理客户端路由，实现页面间的导航

**路由配置**：

| 路径 | 组件 | 描述 |
|------|------|------|
| `/` | Home | 首页，展示设计系统核心特性 |
| `/components` | Components | 组件库文档页面 |
| `/products` | Products | 产品展示页面 |
| `/about` | About | 关于我们页面 |

#### 2.2.4 样式系统 (main.css)

**职责**：定义全局样式、设计令牌和组件样式

**主要内容**：
- CSS 变量定义的设计令牌 (配色、间距、字体等)
- 基础样式重置
- 通用工具类 (容器、网格、Flex 等)
- 基础组件样式 (按钮、卡片、表单等)
- 响应式设计规则
- 动画效果

## 3. 组件调用关系

### 3.1 组件层次结构

```
App.vue (根组件)
├── Navbar (导航栏 - 内联定义)
├── RouterView (路由视图)
│   ├── Home.vue (首页)
│   │   ├── Hero Section
│   │   ├── Features Section
│   │   ├── Stats Section
│   │   └── CTA Section
│   ├── Components.vue (组件库页面)
│   │   ├── Buttons Section
│   │   ├── Cards Section
│   │   ├── Forms Section
│   │   ├── Badges Section
│   │   ├── Grid Section
│   │   └── Design Tokens Section
│   ├── Products.vue (产品页面)
│   │   └── ProductCard (动态渲染)
│   └── About.vue (关于页面)
│       ├── Mission Section
│       ├── Values Section
│       ├── Team Section
│       └── Contact Section
└── Footer (页脚 - 内联定义)
```

### 3.2 组件依赖关系

#### 3.2.1 App.vue 依赖

```
App.vue
├── 依赖: Vue Router (router-link, router-view)
├── 样式依赖: main.css (设计令牌、基础样式)
└── 子组件引用: 页面组件 (通过路由动态加载)
```

#### 3.2.2 页面组件依赖

**Home.vue**：
- 依赖: Vue Router (router-link 用于导航)
- 样式依赖: main.css (卡片、按钮、网格等样式)

**Components.vue**：
- 样式依赖: main.css (所有组件样式)

**Products.vue**：
- 依赖: Vue 3 Composition API (ref)
- 样式依赖: main.css (卡片、标签、网格等样式)

**About.vue**：
- 依赖: Vue 3 Composition API (ref)
- 样式依赖: main.css (卡片、网格等样式)

### 3.3 数据流动

本项目采用单向数据流模式，数据主要来源：

1. **硬编码数据**：直接在组件中定义的静态数据
   - 产品数据 (Products.vue 中)
   - 团队成员数据 (About.vue 中)
   - 特性列表数据 (Home.vue 中)

2. **路由状态**：由 Vue Router 管理的当前路由信息

**数据流动示例**：

```
┌─────────────────────────────────────────────────────────────┐
│                        Products.vue                          │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────┐   │
│  │  const products = ref([                              │   │
│  │    { id: 1, name: '企业版设计系统', ... },          │   │
│  │    { id: 2, name: '专业版组件库', ... },            │   │
│  │    ...                                                │   │
│  │  ])                                                   │   │
│  └──────────────────────────┬──────────────────────────┘   │
│                             │ 单向数据流                     │
│                             ▼                               │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  <div class="card product-card"                      │   │
│  │       v-for="product in products" :key="product.id">│   │
│  │    <div class="product-image">{{ product.icon }}</div>│   │
│  │    <h3>{{ product.name }}</h3>                       │   │
│  │    <p>{{ product.description }}</p>                   │   │
│  │    <div class="product-price">¥{{ product.price }}</div>│   │
│  │  </div>                                               │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

## 4. 路由系统详解

### 4.1 路由配置

```javascript
const routes = [
    { path: '/', component: Home },
    { path: '/components', component: Components },
    { path: '/products', component: Products },
    { path: '/about', component: About }
]

const router = createRouter({
    history: createWebHistory(),
    routes
})
```

### 4.2 导航方式

#### 4.2.1 声明式导航

使用 `<router-link>` 组件进行导航：

```vue
<!-- 导航栏中的链接 -->
<router-link to="/">首页</router-link>
<router-link to="/components">组件库</router-link>
<router-link to="/products">产品展示</router-link>
<router-link to="/about">关于我们</router-link>

<!-- 按钮形式的导航 -->
<router-link to="/components" class="btn btn-primary">查看组件库</router-link>
```

#### 4.2.2 编程式导航

虽然本项目中未使用，但可通过 `router.push()` 进行编程式导航：

```javascript
import { useRouter } from 'vue-router'

const router = useRouter()

// 导航到指定路径
router.push('/products')

// 导航到命名路由（如果配置了）
router.push({ name: 'products' })
```

### 4.3 路由视图渲染

```vue
<!-- App.vue 中的路由视图 -->
<main class="main-content">
    <router-view />
</main>
```

`router-view` 组件会根据当前路由动态渲染对应的页面组件。

## 5. 样式系统架构

### 5.1 设计令牌系统

本项目采用 CSS 变量实现设计令牌系统，集中管理所有设计相关的变量。

**设计令牌分类**：

| 类别 | 示例变量 | 用途 |
|------|----------|------|
| 配色方案 | `--primary`, `--secondary`, `--accent` | 主题色定义 |
| 中性色 | `--gray-50` ~ `--gray-900` | 文本、背景、边框 |
| 语义色 | `--success`, `--warning`, `--error`, `--info` | 状态提示 |
| 间距系统 | `--space-xs` ~ `--space-3xl` | 内边距、外边距 |
| 圆角 | `--radius-sm` ~ `--radius-full` | 边框圆角 |
| 阴影 | `--shadow-sm` ~ `--shadow-xl` | 元素阴影 |
| 字体 | `--font-sans`, `--font-mono` | 字体族 |
| 字体大小 | `--text-xs` ~ `--text-5xl` | 字号 |
| 过渡动画 | `--transition`, `--transition-fast` | 动画时长 |

### 5.2 样式层次结构

```
main.css
├── 设计令牌定义 (CSS Variables)
├── 基础样式重置
├── 容器类
│   ├── .container
│   ├── .container-wide
│   └── .container-narrow
├── 组件样式
│   ├── 按钮 (.btn, .btn-primary, .btn-secondary, .btn-outline)
│   ├── 卡片 (.card)
│   ├── 输入框 (.input)
│   ├── 标签 (.badge, .badge-primary, .badge-success)
│   └── 网格 (.grid, .grid-2, .grid-3, .grid-4)
├── 工具类
│   ├── Flex 工具类 (.flex, .flex-col, .items-center 等)
│   └── 文本工具类 (.text-center, .text-primary 等)
├── 响应式设计 (@media queries)
└── 动画 (@keyframes)
```

### 5.3 组件样式复用

页面组件通过引用 `main.css` 中定义的类来复用样式：

**示例：按钮样式复用**

```vue
<!-- 在任意页面组件中 -->
<button class="btn btn-primary btn-lg">大按钮</button>
<button class="btn btn-outline">边框按钮</button>
```

这些类在 `main.css` 中统一定义，确保了样式的一致性。

## 6. 组件通信机制

### 6.1 组件间通信方式

由于本项目结构相对简单，主要采用以下通信方式：

1. **Props 传递**：本项目中暂无使用，因为页面组件都是独立的
2. **Vue Router 状态**：通过路由参数传递信息（本项目未使用路由参数）
3. **全局状态**：本项目未引入 Vuex/Pinia，无复杂状态管理需求

### 6.2 状态管理策略

**当前策略**：
- 简单状态使用组件内部的 `ref` 或 `reactive`
- 导航状态由 Vue Router 管理
- 无跨组件共享的复杂状态

**未来扩展建议**：
如果需要添加用户认证、购物车等功能，建议引入 Pinia 进行状态管理。

## 7. 测试架构

### 7.1 测试框架

本项目使用 Playwright 进行端到端 (E2E) 测试。

### 7.2 测试覆盖范围

**测试用例**：

| 测试场景 | 描述 |
|----------|------|
| 首页加载 | 验证首页标题、特性卡片、统计卡片数量 |
| 导航功能 | 验证所有页面间的导航是否正常 |
| 组件库页面 | 验证组件库页面显示的组件数量 |
| 产品展示 | 验证产品卡片数量和内容 |
| 关于我们 | 验证团队成员和联系信息 |
| 响应式设计 | 验证移动端显示 |
| 用户流程 | 模拟完整的用户浏览流程 |

### 7.3 测试与组件的关系

测试用例直接针对页面组件和用户交互进行验证，确保：
- 页面正确渲染
- 导航功能正常
- 响应式布局正确
- 用户交互流程完整

## 8. 构建与部署

### 8.1 构建流程

```
源代码
    │
    ▼
┌─────────┐     ┌─────────────┐
│  Vite   │────▶│  依赖解析   │
│  构建   │     │  代码转换   │
└─────────┘     └──────┬──────┘
                        │
                        ▼
                ┌─────────────┐
                │  代码优化   │
                │  - 压缩     │
                │  - 树摇     │
                └──────┬──────┘
                        │
                        ▼
                ┌─────────────┐
                │  输出文件   │
                │  dist/      │
                └─────────────┘
```

### 8.2 构建命令

```bash
# 开发模式
npm run dev

# 生产构建
npm run build

# 预览构建结果
npm run preview
```

## 9. 关键文件说明

### 9.1 main.js (入口文件)

**位置**：`src/main.js`

**核心功能**：
- 创建 Vue 应用实例
- 配置 Vue Router
- 导入全局样式
- 挂载应用到 DOM

**关键代码**：

```javascript
import { createApp } from 'vue'
import { createRouter, createWebHistory } from 'vue-router'
import App from './App.vue'
import './styles/main.css'

// 导入页面
import Home from './pages/Home.vue'
import Components from './pages/Components.vue'
import Products from './pages/Products.vue'
import About from './pages/About.vue'

const routes = [
    { path: '/', component: Home },
    { path: '/components', component: Components },
    { path: '/products', component: Products },
    { path: '/about', component: About }
]

const router = createRouter({
    history: createWebHistory(),
    routes
})

createApp(App).use(router).mount('#app')
```

### 9.2 App.vue (根组件)

**位置**：`src/App.vue`

**核心功能**：
- 提供应用的整体布局
- 包含导航栏和页脚
- 管理移动端菜单状态
- 包含路由视图

**关键特性**：
- 响应式导航栏
- 移动端汉堡菜单
- 平滑滚动导航
- 路由视图容器

### 9.3 main.css (全局样式)

**位置**：`src/styles/main.css`

**核心功能**：
- 定义设计令牌系统
- 提供基础样式重置
- 实现通用组件样式
- 定义响应式断点
- 提供动画效果

## 10. 扩展建议

### 10.1 架构优化建议

1. **引入组件库**：
   - 将内联组件提取为独立组件
   - 创建 `src/components/` 目录
   - 实现按钮、卡片、表单等通用组件

2. **状态管理**：
   - 引入 Pinia 进行状态管理
   - 管理用户状态、主题切换等全局状态

3. **API 层**：
   - 创建 `src/api/` 目录
   - 封装 HTTP 请求
   - 实现数据获取和提交逻辑

4. **类型支持**：
   - 引入 TypeScript
   - 定义组件 Props 类型
   - 定义数据模型类型

### 10.2 性能优化建议

1. **路由懒加载**：
   ```javascript
   const Home = () => import('./pages/Home.vue')
   const Components = () => import('./pages/Components.vue')
   ```

2. **图片优化**：
   - 使用 WebP 格式
   - 实现图片懒加载
   - 配置图片 CDN

3. **缓存策略**：
   - 配置 Service Worker
   - 实现资源预加载

## 11. 总结

本项目采用简洁高效的前端架构，核心特点：

1. **技术栈现代化**：使用 Vue 3 Composition API、Vite 等最新技术
2. **设计系统化**：通过 CSS 变量实现完整的设计令牌系统
3. **组件化程度**：页面级组件化，基础样式类复用
4. **路由管理**：Vue Router 实现客户端路由
5. **测试覆盖**：Playwright 实现完整的 E2E 测试

该架构适合作为设计系统的展示平台，具有良好的可扩展性和维护性。

---

*文档版本：1.0*
*更新日期：2026-04-16*
